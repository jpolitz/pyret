# Real-workload benchmarks: cont vs promise backend

Real Bootstrap/CPO-style programs (not microbenchmarks) comparing the TypeScript
compiler's two stack backends:

- **cont** — continuation/trampoline backend (`--stack-backend cont`, the default), `*.ts.jarr`
- **promise** — Promise/async backend (`--stack-backend promise`), `*.ts.p.jarr`

Each program is stripped to a headless pure-CPU/render loop (no big-bang/reactor): a
`run-frames(N, ...)` driver accumulates an `image-width`/number into a final result so
nothing gets elided.

## Timing model (in-process loop timing)

Each bench brackets **only** the driver loop with `time-now()` — one reading before, one after
the whole `run-frames`/`run` call (not per-iteration) — and prints two lines:

1. the deterministic result (used to check cont≡promise output parity)
2. `LOOP-MS <ms>` — the loop wall-time

This **excludes the jarr-load startup floor** from the number. The floor is ~0.5 s on a warm
file cache (cold first-touch of the 9 MB bundle is ~3.5 s); since the timer starts after load,
neither affects the reported loop time. Frame counts are sized so each loop runs ~3–5 s — enough
to show the per-frame codegen ratio without long waits. Ratios are N-independent, so any N stays
apples-to-apples cont-vs-promise.

| bench | N (frames/queries) | what it exercises |
|---|---|---|
| `bench-spell` | 12 queries | norvig-style corrector: 5000-word BK-tree, bk-search (Levenshtein DP + triangle-inequality prune). String/edit-distance, compute-only. |
| `bench-car-compute` | 22000 | self-driving-car sim physics ticks (numbers only). |
| `bench-car-render` | 500 | same sim with POV rasterize. |
| `bench-lander` | 6000 | lunar-lander: physics + full scene redraw (rotate/scale/overlay/text). Render-dominated. |
| `bench-orbital-compute` | 900 | orbital sim, sim-newton, numbers only. |
| `bench-orbital-ems` | 650 | orbital sim, sim-ems integrator. |
| `bench-orbital-render` | 300 | orbital sim with trace + overlay. Render-dominated. |
| `bench-boids-compute` | 500 | boids/flocking, ticker-only (O(n²) physics, heavy mutable-var + nested closures), no image construction. |
| `bench-boids-compute-data` | 500 | same boids physics re-written over a fully-annotated `data Bird` (method calls + typed fields instead of closures over vars). |
| `bench-boids-raster` | 250 | boids/flocking, ticker + drawer (≈53-overlay scene tree) + real pixel rasterization via `color-at-position`. |
| `bench-vec-methods` | — | vector arithmetic as data-method chains (`v.plus(w).scale(k)`); isolates method-dispatch cost. |
| `bench-matrix` | — | dense matrix multiply over nested lists; field-bound inner loops. |
| `bench-dtree` | 6 tables | ai-library-models decision-tree trainer: recursive `build-tree` over real Pyret `Table`s (`filter-by`/`get-column`/`row-n`), k-means quant thresholds, StringDict `most-common`. |
| `bench-kmeans` | 8 clouds | the bare Lloyd's-algorithm fixpoint (`k-means-clustering`/`find-closest`): map/filter/foldl + exact-rational means + `List` equality. |
| `bench-plagiarism` | 120 all-pairs | plagiarism-library cosine/angle-difference: tokenize (string-explode/fold) + `MutableStringDict` bag-of-words + dot-products over dict keys. |
| `bench-seam` | 2 images | CS019 seam-carving DP: energy matrix + seam propagation via nested `map-triplets`/`map3`/`foldr`, `n` carves per image. |

`bench-car.arr.tmpl` / `bench-orbital.arr.tmpl` are the sources; the compute/render/ems variants
are generated by flipping a `RENDER` flag + frame count (both templates carry the timing harness).

## Build & run

```sh
# from lang/
make tests/async-opt/<bench>.ts.jarr      # cont
make tests/async-opt/<bench>.ts.p.jarr    # promise   (EXTRA_FLAGS=-no-check-mode is the default)
node22 --max-old-space-size=6144 tests/async-opt/<bench>.ts.jarr

# full comparison table (N runs each, median/min/max LOOP-seconds + output parity):
tests/async-opt/run-bench-table.sh 3
```

A full N=3 table runs in ~4–5 min (~90 s at N=1).

## Results (2026-07-03 — Stage 3d: direct cases + direct fields/method dispatch)

First two-box battery: oracles on the primary box, O5 on the new timing box
(`ts-pyret-opus`, node22 v22.17.0, its OWN same-day frozen cont built from
this branch — cont there includes the Stage-1 string-dict gains, so its
ratios read HARSHER than the primary box's; the two boxes' numbers are never
mixed).

- O1 cont byte-parity 16/16 PASS — load-bearing here: the desugar-post-tc
  change (SCasesElse keeps its compiled annotation) is this stage's one
  shared-pipeline edit, and cont bytes did not move.
- O3 13230 green, O4 12798 green, mf oracle green. A/B on cases-heavy
  bench-dtree: output identical with -no-direct-cases -no-direct-fields;
  static dict-read sites appear only in the enabled build.
- O5 (timing box, N=5): parity 16/16, geomean **1.019** vs its harsher cont.
  vec-methods 0.66, matrix 0.88, dtree 0.96. plagiarism 1.42 there =
  the string-dict-enabled cont side (its promise absolute times match the
  primary box); the tier stage remains its fix.
- O6: skipped this stage — the timing box turned out to run the exec SUITE
  ~13x slower than the primary (CPU and disk both benchmark FINE; suite-
  specific pathology, under investigation), so whole-suite wall-clock stays
  a primary-box oracle and resumes with the next stage's battery. Division
  of labor recorded: timing box = O5 bench tables only.

Discovered during this battery and fixed: the S3.7 test patch had placed an
import mid-file (Pyret parse error) — caught by the timing box's fresh
build; amended before landing.

## Results (2026-07-03 — Stage 3c': opt-facts provides section + cross-module method flatness)

The schema-tagged `opt-facts` section (rule-3 design commit) + imported
method flatness (`-no-imported-method-flat`).

- New oracle `make ts-mf-test` all green: mf-01-dict 3-way differential sound
  with flattening FIRED (guards 26 opt vs 33 base); the version-skew triple
  (strip section / unknown field / schema-99) each run byte-identical output
  with the conservative guard counts restored; the `Number%(p)`
  refinement-alias pin holds (importer method stays non-flat; `:: Number`
  control twin flattens). Fixture lesson recorded: `provide *` does not
  export type aliases — `provide-types *`.
- O1 cont byte-parity 16/16 PASS. O3 13210 green, O4 12778 green.
- O5 N=5: parity 16/16, geomean **1.040** (from 1.048). spell 0.86.
  plagiarism read 1.26 (up from 1.21) but the mechanism demonstrably fires
  there — emitted guards drop 46 → 40 with imported flatness on — so the
  reading is same-day noise; its structural drop waits on the tier stage.
- O6 interleaved N=3 medians: cont 234.6 s vs promise 247.2 s → p/c 1.05
  (1.03–1.05 band across the last two stages).

## Results (2026-07-03 — Stage 3b/3c: operator weakening + in-module method flatness)

Two commits measured together (same-day frozen cont, fresh caches, jarr
freshness pinned by `_plus_nums` in emitted module JS):

- Feature smokes: bench-matrix emits `_plus_nums`/`_times_nums`;
  vec-methods' method table converges dot=1/magsq=2/mag=3 flat with 4 flat
  method-apps (`PYRET_METHOD_DEBUG=1`); numflat-sound output identical with
  all three levers off (custom `_plus` still dispatches).
- O1 cont byte-parity 16/16 PASS. O3 13207 green (incl. the new
  functional-extend-strips-brand pins), O4 12775 green.
- O5 N=5: parity 16/16, geomean **1.048** (from 1.117). Movers:
  **matrix 1.27 → 0.85**, **vec-methods 1.08 → 0.84**, spell 0.92,
  boids-compute-data 1.22 → 1.08. Still high and expected to stay until the
  tier work: car-compute 1.26, orbital-compute 1.23, boids-compute 1.23,
  plagiarism 1.21 (call machinery / representation costs).
- O6 interleaved N=3 medians: cont 234.0 s vs promise 240.5 s → **p/c 1.03**
  (from 1.08).

Method-flatness note for later stages: constructor apps inside method bodies
are currently not flat (vec-methods' plus/minus/scale stay INF in the table
for that reason) — worth revisiting alongside the tier analysis.

## Results (2026-07-02 — Stage 3a: type-flow ann elision + operator-globals registration)

Two commits measured together (the second is behaviorally inert): the
upper-bound type-flow pass with redundant-`_checkAnn` elision
(`-no-ann-elision`), and the 21 flat operator globals registered in all four
sources of truth (no consumer yet — the weakening pass lands next).

- Feature A/B on bench-anns (fully-typed cases workload): 17 → 11 emitted
  `R._cA` checks, output identical with `-no-ann-elision`. (`R._cA` is the
  emitted-check signature to grep for — `_checkAnn` only appears in the
  runtime.)
- O1 cont byte-parity 16/16 PASS. O3 13203 green, O4 12771 green (suites grew
  by the new ann-elision/refinement/#1532 pins).
- O5 N=5: parity 16/16, geomean **1.117** (from 1.133): matrix 1.33 → 1.27,
  vec-methods 1.08; annotation-light benches unmoved (plagiarism/boids wobble
  within the day's noise band).
- O6 interleaved N=3 medians: cont 231.1 s vs promise 249.0 s → p/c 1.08
  (statistically flat vs Stage 2's 1.07).

## Results (2026-07-02 — Stage 2: appBody patch in the lazy data-constructor shim)

Runtime-only fix (see the commit): the safe-for-space token driver calls
`.appBody`, which the lazy constructor shim left pointing at the Function()
synthesizer, so every tail call to a data constructor re-synthesized it.
Embedded-runtime landmine drill: `make build/phaseA/js/runtime-async.js`,
`build/ts-compiler/js/runtime-async.js` refreshed, all 16 promise jarrs +
suite jarrs rebuilt (freshness pinned by grepping the new comment in a jarr
and its absence in frozen cont).

- O1 cont byte-parity 16/16 PASS; O3 13176 green; O4 12751 green.
- O5 N=5: parity 16/16, geomean **1.133** (flat vs 1.130 — expected: the
  curated benches barely tail-call constructors; plagiarism 1.25).
- O6 interleaved N=3 medians: cont 229.1 s vs promise 244.5 s →
  **p/c 1.19 → 1.07**. The whole-suite constructor traffic is where this bug
  lived (on the discovery branch it was measured at 2.31% of samples via
  makeConstructor alone, post-gen-levers; here, pre-tiers, the tail-token
  driver is hot everywhere, so the payoff is larger).

## Results (2026-07-02 — Stage 1: string-dict library pass)

`string-dict.js` read fast path on frozen dicts + typeof-gated arg checks +
keys-list memo (see the commit message for the full design). Promise caches
cleaned, all 16 promise jarrs + suite jarrs rebuilt (freshness pinned by
grepping `$fastDict` in the new jarrs and its absence in the frozen-cont ones).
Cont side stays the frozen Stage-0 build.

- O1 cont byte-parity: 16/16 PASS (trove JS is embedded verbatim by both
  compilers, so parity is structural — confirmed anyway).
- O3 promise main2: 13176 green (covers string-dict error messages and
  key-order pins). O4 exec: 12751 green.
- O5 N=5: parity 16/16, geomean **1.130** (from 1.134). The pass is a targeted
  one: **plagiarism 1.46 → 1.24** (its hot path is freeze() + per-key
  get-value/has-key dot-products). dtree printed 1.16 in the table but an
  interleaved re-check gives p/c ≈ 1.06 with cont itself wobbling 1.12–1.31 —
  same-day noise band, not a regression. Remaining big ratios (matrix 1.33,
  boids 1.29, orbital-compute 1.26) are call-machinery/representation costs —
  later stages' territory.

## Results (2026-07-02 — re-derivation Stage 0: baseline lockdown at 8674a57c6)

Session-frozen baseline for the re-derivation campaign (branch `promise-rederive`;
spec `~/promise-backend-rederivation-spec.md`). Compilers rebuilt from the clean
baseline commit; both promise caches cleaned; all 16 bench pairs + suite jarrs
rebuilt from scratch. **The cont jarrs built here are the frozen-cont baseline
for every later stage** (backup copy in `~/frozen-cont-jarrs/`).

Oracles at baseline:

- O1 cont byte-parity (`parity-test.sh`): **16/16**. (The *promise* byte-parity
  script `parity-promise-test.sh` fails 13/17 — pre-existing: conditional-await
  entered the TS compiler before this baseline, commit 1914009cf, and was never
  mirrored in the phaseA `.arr` compiler. The gate for this campaign is cont
  parity; promise-side semantic load is carried by run-parity + the suites.)
- O2 cont main2: 13184 green. O3 promise main2: **13176 green**.
- O4 promise main2-exec: 12751 green.
- O6 whole-suite exec wall-clock, interleaved N=3 medians, `-check-all` both
  sides: cont 228.4 s vs promise 272.5 s → **p/c = 1.19**.

Curated table N=5 (`run-bench-table.sh 5`), parity 16/16 OK, **geomean 1.134**:

| benchmark | out | cont med | prom med | p/c |
|---|---|---|---|---|
| bench-spell | 73 | 3.12 | 2.89 | 0.93 |
| bench-car-compute | 14829840 | 2.86 | 3.50 | 1.22 |
| bench-car-render | 400000 | 2.94 | 2.92 | 0.99 |
| bench-lander | 4800000 | 2.67 | 2.65 | 0.99 |
| bench-orbital-compute | 2959 | 2.41 | 3.19 | 1.32 |
| bench-orbital-ems | 390000 | 1.79 | 1.73 | 0.97 |
| bench-orbital-render | 180000 | 2.87 | 3.24 | 1.13 |
| bench-boids-compute | 236561 | 2.66 | 3.43 | 1.29 |
| bench-boids-compute-data | 236561 | 2.83 | 3.40 | 1.20 |
| bench-boids-raster | 250 | 2.62 | 3.02 | 1.15 |
| bench-vec-methods | 250590 | 2.65 | 3.14 | 1.18 |
| bench-matrix | 640615 | 3.16 | 4.37 | 1.38 |
| bench-dtree | 829 | 1.15 | 1.19 | 1.03 |
| bench-kmeans | 40568 | 1.77 | 1.93 | 1.09 |
| bench-plagiarism | 583869000 | 1.36 | 1.98 | 1.46 |
| bench-seam | 478707 | 2.19 | 2.14 | 0.98 |

Starting point matches the campaign expectation (~1.18 whole-suite, curated
geomean well above 1; worst: plagiarism 1.46, matrix 1.38, orbital-compute 1.32).

## Results (2026-06-26, in-process loop timing)

N=3, median LOOP-seconds (startup floor excluded), `node22 --max-old-space-size=6144`, no
concurrent builds. All 9 are **output-identical** between backends (parity OK). promise backend
has the effect-tail-call optimization (`SetTailVisitor`, default-on) and conditional-await active.

| benchmark | cont med | prom med | p/c | parity |
|---|---|---|---|---|
| spell           | 4.51 | 4.21 | **0.93** | OK |
| car-compute     | 3.96 | 5.14 | **1.30** | OK |
| car-render      | 4.20 | 4.05 | 0.96 | OK |
| lander          | 4.06 | 4.20 | 1.03 | OK |
| orbital-compute | 3.61 | 4.59 | **1.27** | OK |
| orbital-ems     | 2.72 | 2.61 | 0.96 | OK |
| orbital-render  | 4.63 | 4.80 | 1.04 | OK |
| boids-compute   | 3.91 | 5.00 | **1.28** | OK |
| boids-raster    | 3.72 | 4.43 | **1.19** | OK |

**Geomean p/c ≈ 1.10×.** Same clean split as the original wall-clock run: render / string /
functional workloads at parity or promise-faster (spell, car-render, orbital-ems, orbital-render,
lander); compute-bound, mutable-var-heavy loops ~1.2–1.3× slower (car-compute, orbital-compute,
boids-compute, boids-raster) — the mutable-var (`box.$var`) hot-loop tax inside async closures.
Because the floor is now excluded, these loop ratios are truer than the wall-clock table below,
where the shared startup floor diluted `p/c` toward 1.0.

**Never run timings concurrently with builds or other node processes** — heavy jarr compiles peak
multi-GB and will OOM a small box; the benches themselves peak only ~170 MB.

---

> **⚠️ Superseded below (kept for reference): original wall-clock results, pre in-process timing.**
> These timed the whole process with `/usr/bin/time` (startup floor **included**) and used the
> original large frame counts (e.g. orbital-render 1500 frames ≈ 198 s). Current methodology and
> numbers are above; the "measurement caveat" at the very bottom is what motivated the switch.

# Real-workload benchmarks: cont vs promise backend

Real Bootstrap/CPO-style programs (not microbenchmarks) comparing the TypeScript
compiler's two stack backends:

- **cont** — continuation/trampoline backend (`--stack-backend cont`, the default), `*.ts.jarr`
- **promise** — Promise/async backend (`--stack-backend promise`), `*.ts.p.jarr`

Each program is stripped to a headless pure-CPU/render loop (no big-bang/reactor): a
`run-frames(N, ...)` driver accumulates an `image-width`/number into a final result so
nothing gets elided. The benches:

| bench | what it exercises |
|---|---|
| `bench-spell` | norvig-style corrector: deserialize a 5000-word BK-tree, bk-search (Levenshtein DP + triangle-inequality prune) over 40 misspellings. String/edit-distance, compute-only. |
| `bench-car-compute` | self-driving-car sim, 200000 physics ticks (numbers only). |
| `bench-car-render` | same sim, 4000 ticks with POV rasterize. |
| `bench-lander` | lunar-lander: 6000 frames physics + full scene redraw (rotate/scale/overlay/text). Render-dominated. |
| `bench-orbital-compute` | orbital sim, sim-newton 3000 frames, numbers only. |
| `bench-orbital-ems` | orbital sim, sim-ems integrator 2500 frames. |
| `bench-orbital-render` | orbital sim, 1500 frames with trace + overlay. Render-dominated. |
| `bench-boids-compute` | boids/flocking sim, ticker-only (O(n²) flocking physics, heavy mutable-var + nested closures), 3000 frames, no image construction. |
| `bench-boids-render` (`bench-boids-raster.arr`) | boids/flocking sim, 500 frames of ticker + drawer (≈53-overlay scene tree) + real pixel rasterization via `color-at-position`. Still runs the ticker every frame, so the compute tax is present. |

`bench-car.arr.tmpl` / `bench-orbital.arr.tmpl` are the sources; the compute/render/ems
variants are generated by flipping a `RENDER` flag + frame count. `bench-boids-compute.arr`
shares the 205-line core of `bench-boids.arr` with a ticker-only driver; the render row below
is measured on `bench-boids-raster.arr`.

## Build & run

```sh
# from lang/
make tests/async-opt/<bench>.ts.jarr      # cont
make tests/async-opt/<bench>.ts.p.jarr    # promise   (EXTRA_FLAGS=-no-check-mode is the default)
node22 --max-old-space-size=6144 tests/async-opt/<bench>.ts.jarr

# full comparison table (N runs each, median/min/max + output parity):
tests/async-opt/run-bench-table.sh 3
```

## Results (2026-06-26)

N=3, median wall-clock seconds, `node22 --max-old-space-size=6144`, no concurrent builds.
All 9 are **output-identical** between backends (parity OK). Built with the effect-tail-call
optimization (`SetTailVisitor`, default-on) **and** conditional-await both active in the
promise backend.

| benchmark | cont med | prom med | p/c | parity |
|---|---|---|---|---|
| spell           | 14.01 | 12.82 | **0.92** | OK |
| car-compute     | 31.52 | 40.18 | **1.27** | OK |
| car-render      | 28.01 | 27.28 | 0.97 | OK |
| lander          |  4.46 |  4.50 | 1.01 | OK |
| orbital-compute | 11.64 | 15.18 | **1.30** | OK |
| orbital-ems     | 60.06 | 58.51 | 0.97 | OK |
| orbital-render  | 198.07 | 196.98 | 0.99 | OK |
| boids-compute   | 21.12 | 27.00 | **1.28** | OK |
| boids-render    |  7.47 |  8.67 | **1.16** | OK |

**Geomean p/c ≈ 1.09×.** Clean split:

- **Render / string / functional workloads: parity or promise-faster** (spell, car-render,
  orbital-ems, orbital-render).
- **Compute-bound, mutable-var-heavy loops: promise ~1.16–1.30× slower** (car-compute,
  orbital-compute, boids-compute, boids-render) — the mutable-var (`box.$var` read/write)
  hot-loop tax inside async closures. This is the promise backend's one remaining weakness;
  everything else is at or below cont. boids is the worst case: its ticker is almost pure
  `var`-mutation in nested closures, so even the render variant (which still runs the ticker
  every frame) carries the tax. boids-compute (1.28×) reproduces the earlier hand-bisected
  ticker-only ratio (~1.33×).

## Measurement caveat (important for optimization loops)

Each bench time = **fixed startup floor (~3–5 s, jarr bundle load+init, backend-neutral)** +
N × per-frame loop cost. The cont-vs-promise codegen difference lives **only in the loop**, so
the shared startup floor dilutes `p/c` toward 1.0. Short benches understate the real gap:

- `orbital-compute` loop-only ratio is ~**1.47×** (wall 1.30; floor ≈ 35% of the run).
- `boids-render` (7.5 s) is ~50% startup floor; back it out and the loop-only ratio is ~**1.33×**,
  matching `boids-compute` — its 1.16× wall understates the real per-frame tax.
- `lander` (4.5 s) is ~90% startup floor, so its 1.01× says little about the loop.
- Long benches (`orbital-ems` 60 s, `orbital-render` 198 s) have negligible floor — wall ratio ≈ true ratio.

To iterate fast *and* honestly, time the `run-frames(...)` call internally and report loop-seconds
(excludes startup) — then small-N runs measure the same ratio as large-N, just noisier.

**Never run timings concurrently with builds or other node processes** — it contaminates results
badly (heavy jarr compiles peak multi-GB and will OOM a small box; the benches themselves peak
only ~170 MB).
